// limit the system resources against which nextflow schedules processes
executor {
    name   = 'slurm'
    scratch = '/scratch'
 }

process {
    queue = 'fast'
    cpus = 6
    memory = '128 GB'
}
